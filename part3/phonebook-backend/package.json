{
  "name": "phonebook_backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:ui": "rm -rf /build && cd ../phonebook-frontend/ && npm run build && cp -r build ../phonebook-backend"
  },
  "author": "kiennguyen",
  "license": "ISC",
  "devDependencies": {
    "@flydotio/dockerfile": "^0.3.3",
    "morgan": "^1.10.0",
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
